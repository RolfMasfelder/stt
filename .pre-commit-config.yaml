# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
fail_fast: false  # Don't stop on first failure, let all tools run
default_stages: [pre-commit]

repos:
  # Ruff: linting + formatting (replaces black, isort, flake8)
  - repo: https://github.com/charliermarsh/ruff-pre-commit
    rev: "v0.12.5"
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format

  # General file quality checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: check-merge-conflict
      - id: check-docstring-first
      - id: check-toml
      - id: check-yaml
        args: [--allow-multiple-documents]
        exclude: ^k8s/helm/
      - id: check-json
      - id: check-added-large-files
        args: ['--maxkb=5120']
      - id: detect-private-key
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: debug-statements
      - id: name-tests-test
        args: [--pytest-test-first]
      - id: requirements-txt-fixer

  # Dart formatting (runs inside the flutter Docker service)
  # Formats mobile/ in-place; pre-commit detects changed files and blocks commit.
  # User must re-stage formatted files and commit again.
  - repo: local
    hooks:
      - id: dart-format
        name: dart format
        language: system
        entry: docker compose run --rm --no-deps -T -w /app flutter dart format .
        files: \.dart$
        pass_filenames: false
