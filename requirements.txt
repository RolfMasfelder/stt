# -- PyTorch CPU-only (must be installed before pyannote) --
--extra-index-url https://download.pytorch.org/whl/cpu
boto3>=1.35.0                # S3-compatible object storage (ADR-11)
cryptography>=44.0.0         # AES-256-GCM encryption at rest (ADR-08)

# -- Django + REST Framework --
django==5.2
django-cors-headers==4.7.0   # CORS middleware
django-oauth-toolkit==3.0.1  # OAuth2 Provider (RFC 6749/7636)
django-q2==1.9.0             # Task queue with DB broker
djangorestframework==3.16.0
drf-spectacular==0.28.0      # OpenAPI 3.0 schema generation

# -- ML Stack --
faster-whisper==1.2.1
gunicorn==23.0.0             # WSGI production server
numpy==2.4.2         # wird von sounddevice gebraucht
psycopg[binary]==3.2.6       # PostgreSQL adapter
pyannote.audio==4.0.4 # speaker diarization
python-dotenv==1.2.1 # for loading .env files
requests==2.32.5
sounddevice==0.5.5   # falls du selbst Audio aufzeichnen willst
torch==2.11.0+cpu
torchaudio==2.11.0+cpu
torchcodec==0.11.0
