# -- Django + REST Framework --
boto3>=1.43.40                # S3-compatible object storage (ADR-11)
cryptography>=49.0.0         # AES-256-GCM encryption at rest (ADR-08)

# -- Django + REST Framework --
django==5.2.15
django-cors-headers==4.7.0   # CORS middleware
django-oauth-toolkit==3.3.0  # OAuth2 Provider (RFC 6749/7636)
django-prometheus==2.5.0     # Prometheus metrics for Django
django-q2==1.10.0             # Task queue with DB broker
djangorestframework==3.17.1
drf-spectacular==0.29.0      # OpenAPI 3.0 schema generation

# -- Runtime --
gunicorn==26.0.0             # WSGI production server
numpy==2.5.1
psycopg[binary]==3.3.4       # PostgreSQL adapter
python-dotenv==1.2.1         # for loading .env files
requests==2.34.2

# ci-test placeholder
