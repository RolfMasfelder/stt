services:
  stt-server:
    build:
      context: .
      secrets:
        - hf_token
    container_name: stt-server
    env_file:
      - .env
    ports:
      - "8090:8090"
    volumes:
      - .:/app:z
    environment:
      - PYTHONPATH=/app
      - LM_STUDIO_HOST=${LM_STUDIO_HOST}
      - LM_STUDIO_PORT=${LM_STUDIO_PORT}
      - LM_STUDIO_MODEL=${LM_STUDIO_MODEL}
      - WHISPER_MODEL=${WHISPER_MODEL}
      - WHISPER_DEVICE=${WHISPER_DEVICE}
      - WHISPER_API_URL=${WHISPER_API_URL}
      - HF_STT_TOKEN=${HF_STT_TOKEN}
      - LOG_LEVEL=${LOG_LEVEL}
    networks:
      - stt-network
    profiles:
      - production
    command: ["python", "-m", "uvicorn", "stt.server:app", "--host", "0.0.0.0", "--port", "8090"]

  # CLI service for one-off commands via docker-compose exec/run
  stt-cli:
    build:
      context: .
      secrets:
        - hf_token
    container_name: stt-cli
    env_file:
      - .env
    volumes:
      - .:/app:z
      - ${AUDIO_INPUT_DIR:-./data/audio}:/app/data/audio:z
      - ${OUTPUT_DIR:-./data/output}:/app/data/output:z
    environment:
      - PYTHONPATH=/app
      - STT_SERVER_URL=${STT_SERVER_URL}
      - LOG_LEVEL=${LOG_LEVEL}
    network_mode: host
    profiles:
      - cli
    command: ["python", "-m", "stt"]

  # Development service with host networking
  stt-dev:
    build:
      context: .
      secrets:
        - hf_token
    container_name: stt-dev
    env_file:
      - .env
    volumes:
      - .:/app:z
      - ${AUDIO_INPUT_DIR:-./data/audio}:/app/data/audio:z
      - ${OUTPUT_DIR:-./data/output}:/app/data/output:z
    environment:
      - PYTHONPATH=/app
      - LM_STUDIO_HOST=${LM_STUDIO_HOST}
      - LM_STUDIO_PORT=${LM_STUDIO_PORT}
      - LM_STUDIO_MODEL=${LM_STUDIO_MODEL}
      - WHISPER_MODEL=${WHISPER_MODEL}
      - WHISPER_DEVICE=${WHISPER_DEVICE}
      - WHISPER_API_URL=${WHISPER_API_URL}
      - HF_STT_TOKEN=${HF_STT_TOKEN}
      - LOG_LEVEL=${LOG_LEVEL}
      - DEVELOPMENT_MODE=${DEVELOPMENT_MODE}
      - DEBUG=${DEBUG}
    network_mode: host
    profiles:
      - dev
    command: bash

  # Remote Whisper transcription server (faster-whisper-server)
  # Run on a more powerful machine for faster transcription
  whisper-server:
    image: fedirz/faster-whisper-server:latest-cpu
    container_name: whisper-server
    ports:
      - "8001:8000"
    environment:
      - WHISPER__MODEL=${WHISPER_MODEL:-small}
      - WHISPER__DEVICE=${WHISPER_DEVICE:-cpu}
    networks:
      - stt-network
    profiles:
      - whisper-remote

networks:
  stt-network:
    driver: bridge

secrets:
  hf_token:
    environment: "HF_STT_TOKEN"
