# 1. Normal development on local git server
git add .
git commit -m "feat: add new invoice validation"
git push origin main

# 2. When feature is ready for CI/CD testing
git push github main
# This triggers GitHub Actions automatically

############################################

# Create feature branch
git checkout -b feature/ci-cd-setup

# Work locally, push to local server frequently
git push origin feature/ci-cd-setup

# When ready for CI testing, push to GitHub
git push github feature/ci-cd-setup

# After CI passes, merge and push to both
git checkout main
git merge feature/ci-cd-setup
git push origin main
git push github main
